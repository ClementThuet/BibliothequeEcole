{% extends 'base.html.twig' %}
{% block title %}Accueil{% endblock %}
{% block body %}
    <h1> Bienvenue dans la bibliothèque de l'école de Boursonne</h1>
    {{pupil.lastName}}
    {{pupil.firstName}}
    {{pupil.dateOfBirth|date('d-m-Y')}}
    {{pupil.grade}}
    {%if pupil.image.url is defined %}
        <img src="{{pupil.image.url}}" alt="{{pupil.image.alt}}">
    {%endif%}
    Dernier emprunt :
    {{lastBorrow.book.title}} le {{lastBorrow.date|date('d-m-Y')}}   
{% endblock %}